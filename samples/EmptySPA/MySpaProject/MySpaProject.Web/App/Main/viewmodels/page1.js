define([], function () {

    return function () {
        var that = this;

        that.activate = function () {
            that.loadRegions();
        };

        that.loadRegions = function () {

        };
    };
});