using System;

namespace Abp.Web.Utils.Extensions
{
    /// <summary>
    /// String class extensions.
    /// </summary>
    internal static class StringExtensions
    {
        ///// <summary>
        ///// Converts first character of given string to lower case.
        ///// </summary>
        ///// <param name="str">A string</param>
        ///// <returns>Same string as first char lowered</returns>
        //public static string ToFirstCharacterLower(this string str)
        //{
        //    if (String.IsNullOrEmpty(str) || Char.IsLower(str, 0))
        //    {
        //        return str;
        //    }

        //    return Char.ToLowerInvariant(str[0]).ToString() + str.Substring(1);
        //}

        ///// <summary>
        ///// Converts first character of given string to upper case.
        ///// </summary>
        ///// <param name="str">A string</param>
        ///// <returns>Same string as first char uppered</returns>
        //public static string ToFirstCharacterUpper(this string str)
        //{
        //    if (String.IsNullOrEmpty(str) || Char.IsUpper(str, 0))
        //    {
        //        return str;
        //    }

        //    return Char.ToUpperInvariant(str[0]).ToString() + str.Substring(1);
        //}
    }
}
